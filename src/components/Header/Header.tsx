import React from "react";

import { Container } from "./styles";

export const Header: React.FC = () => {
  return (
    <Container>
      <span>Registrar funcionário</span>
      <span>Tabelas e cálculos do IRRF</span>
    </Container>
  );
};
